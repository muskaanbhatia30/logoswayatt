{
  "name": "logo-server",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "mocha app.js --timeout 10000 --reporter mocha-junit-reporter --exit",
    "coverage": "nyc --reporter cobertura --reporter lcov --reporter text --reporter json-summary  mocha app.js --timeout 10000  --exit"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mocha": "^11.7.2",
    "mocha-junit-reporter": "^2.2.1",
    "nyc": "^17.1.0"
  }
}
